# Refresh UAT compares the whole range

A valid refresh can preserve the base valuation while independently changing
the low or high scenario. Operational UAT must compare the complete scenario
range and exact expected candidate, not require movement in one chosen point.

Recall when: activation tests reject a source-bound candidate only because its
base case is unchanged.
